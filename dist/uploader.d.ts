/**
 * Uploads a base64 image to imgur
 *
 * @param {String} image the base64 encoded image
 * @returns {String} the link to the uploaded image
 */
export default function (image: string): Promise<string>;
